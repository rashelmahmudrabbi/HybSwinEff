# LIME Explainability for HybSwinEff

LIME (Local Interpretable Model-agnostic Explanations) is used to highlight
the regions of blood smear images that drive the model's predictions.

## Usage
```bash
pip install -r requirements.txt
python run_lime.py
```